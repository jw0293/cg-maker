spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
    include: oauth, db

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

server:
  port: 8084

logging:
  level:
    org:
      springframework: debug
